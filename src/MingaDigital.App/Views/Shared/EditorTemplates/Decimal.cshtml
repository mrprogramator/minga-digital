@model Decimal?

<input type="text" class="form-control @ViewData["class"]"
  placeholder="@ViewData["placeholder"]"
  autofocus="@ViewData["autofocus"]"
  name="@Html.NameFor(m => m)"
  value="@Html.ValueFor(m => m)">