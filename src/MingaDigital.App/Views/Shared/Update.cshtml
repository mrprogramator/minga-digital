@model UsuarioChangePasswordModel

@{
    ViewBag.Title = "Reestablecer Password";
    
    ViewBag.CancelUrl = Url.Action("Detail", new { id = Model.UsuarioId });
}

@Html.Partial("BasicEditor", Model)